// This is to handle link sharing and hard browser refresh from stock chart modal.
type Props = {
  params: { symbol: string; limit: string }
}

export default function ChartPage(props: Props) {
  return <div>non-modal chart page</div>
}
